#include<stdio.h>

int LCM(int array1[],int size){

}

int GCD(int arr[],int size){
    int result = arr[0];
    for (int i = 1; i < n; i++)
    {
        result = gcd(arr[i], result);

        if(result == 1)
        {
           return 1;
        }
    }
    return result;
}


int main(){
int m,n;
scanf("%d %d",&n,&m);
int array1[n];
int array2[m];

}
